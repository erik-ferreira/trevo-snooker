import styled from "styled-components/native"

export const NotFoundPlayersContainer = styled.Text`
  color: ${(props) => props.theme.colors.blue["800"]};
  font-family: ${(props) => props.theme.fonts.orbi.semiBold};
  font-size: 32px;
  text-align: center;
`
