import styled from "styled-components/native"

export const BoxCheckContainer = styled.View`
  width: 32px;
  height: 32px;
  border-radius: 16px;
  background-color: ${(props) => props.theme.colors.section};

  align-items: center;
  justify-content: center;
`
